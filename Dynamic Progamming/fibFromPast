import java.util.*;
class Main {
    public static void fibFromPast(String[] args) {
        Scanner sc = new Scanner(System.in);
        int s=sc.nextInt();
        int[] dp=new int[s+1];
        dp[0]=0;
        dp[1]=1;
        for(int i=2;i<=s;i++){
            dp[i]=dp[i-1]+dp[i-2];
        }
        System.out.print(Arrays.toString(dp));
        
    }
}
