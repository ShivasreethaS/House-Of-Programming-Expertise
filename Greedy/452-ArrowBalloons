class Solution {
    public int findMinArrowShots(int[][] points) {
        Arrays.sort(points,(a,b)->Integer.compare(a[1],b[1]));
        int arrow=1,cur=points[0][1];
        int n=points.length;
        for(int i=1;i<n;i++){
            if(cur<points[i][0]){
                arrow++;
                cur=points[i][1];
            }
        }
        return arrow;
    }
}
